version: 2
updates:
  - package-ecosystem: 'github-actions'
    directory: '/'
    schedule:
      interval: 'monthly'
    # Let new releases bake for a week before updating to them
    cooldown:
      default-days: 7
    groups:
      actions:
        patterns:
          - '*'
